spring.application.name=backend

# DATABASE (Aiven Cloud MySQL)
spring.datasource.url=jdbc:mysql://reservex-lochithya12-14c4.b.aivencloud.com:26989/defaultdb?sslMode=REQUIRED&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_BLZPtQ1ZigImkB4OFMR
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.open-in-view=false

# SERVER - HTTPS
server.port=8443
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=changeit
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=reservex

# JWT (kept for admin portal)
jwt.secret=${JWT_SECRET:ReserveXColomboBookFairSecretKeyForJWTTokenGeneration2024}
jwt.expiration-ms=86400000

# ASGARDEO OIDC (for vendor portal)
# Replace YOUR_ORG_NAME with your Asgardeo organization name
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://api.asgardeo.io/t/reservex/oauth2/jwks

# ADMIN USER SEEDING
app.seed.admin-email=${ADMIN_EMAIL:admin@bookfair.lk}
app.seed.admin-password=${ADMIN_PASSWORD:admin123}

# MAIL
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:pabodha726@gmail.com}
spring.mail.password=${MAIL_PASSWORD:tstksyjsepptqjoz}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# LOGGING
logging.level.org.springframework.security=INFO
logging.level.org.springframework.mail=INFO
logging.level.com.reservex=INFO

logging.level.org.springframework.security.oauth2=DEBUG

logging.level.org.springframework.security.oauth2.jwt=TRACE







